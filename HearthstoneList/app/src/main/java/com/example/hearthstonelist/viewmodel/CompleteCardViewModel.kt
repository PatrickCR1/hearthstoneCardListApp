package com.example.hearthstonelist.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class CompleteCardViewModel(application: Application): AndroidViewModel(application) {
}